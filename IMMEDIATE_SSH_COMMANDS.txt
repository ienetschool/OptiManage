## IMMEDIATE FIX - Run these commands in SSH terminal:

cd /var/www/vhosts/vivaindia.com/opt.vivaindia.sql

# 1. Check what's actually running on port 8080
netstat -tlnp | grep :8080
ss -tlnp | grep :8080

# 2. Check PM2 logs to see the actual error
pm2 logs optistore-main --lines 10

# 3. Stop PM2 and start manually to see real error
pm2 delete optistore-main
pm2 kill

# 4. Start manually with debug output
export NODE_ENV=production
export PORT=8080
export FORCE_PRODUCTION=true
export DATABASE_URL="mysql://ledbpt_optie:g79h94LAP@localhost:3306/opticpro"

# 5. Try direct node start
node --loader tsx/esm server/index.ts

## This will show you the real error preventing port 8080 from working.
## Send me the output from these commands.