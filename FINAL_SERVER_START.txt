FINAL SERVER START - CORRECT PATH
=================================

Run these commands in your SSH terminal:

# 1. Go to the CORRECT directory 
cd /var/www/vhosts/vivaindia.com/opt.vivaindia.sql

# 2. Check what's there
ls -la

# 3. Kill existing processes
pkill -f tsx
pkill -f node

# 4. Create server directory if needed
mkdir -p server

# 5. Start server (create basic one if files missing)
NODE_ENV=production PORT=8080 FORCE_PRODUCTION=true DATABASE_URL='mysql://ledbpt_optie:g79h94LAP@localhost:3306/opticpro' npx tsx server/index.ts

# This will either start your existing server or show what's missing