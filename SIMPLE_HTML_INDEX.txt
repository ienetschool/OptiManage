CREATE SIMPLE FRONTEND - Run after server is working:
==================================================

# Create a simple frontend
mkdir -p server/public
cat > server/public/index.html << 'HTMLEOF'
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OptiStore Pro - Medical Practice Management</title>
    <style>
        body { 
            font-family: Arial, sans-serif; 
            max-width: 1200px; 
            margin: 0 auto; 
            padding: 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: white;
        }
        .container { 
            background: rgba(255,255,255,0.1); 
            padding: 30px; 
            border-radius: 15px; 
            backdrop-filter: blur(10px);
        }
        .dashboard { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; margin: 30px 0; }
        .card { 
            background: rgba(255,255,255,0.2); 
            padding: 20px; 
            border-radius: 10px; 
            text-align: center;
            backdrop-filter: blur(5px);
        }
        .card h3 { margin: 0 0 10px 0; color: #fff; }
        .card .number { font-size: 2em; font-weight: bold; color: #4CAF50; }
        .status { 
            background: rgba(76, 175, 80, 0.2); 
            padding: 10px; 
            border-radius: 5px; 
            margin: 20px 0;
            border-left: 4px solid #4CAF50;
        }
        h1 { text-align: center; color: #fff; margin-bottom: 10px; }
        .subtitle { text-align: center; opacity: 0.8; margin-bottom: 30px; }
    </style>
</head>
<body>
    <div class="container">
        <h1>🏥 OptiStore Pro</h1>
        <p class="subtitle">Medical Practice Management System</p>
        
        <div class="status">
            <strong>✅ Server Status:</strong> <span id="status">Loading...</span>
        </div>

        <div class="dashboard">
            <div class="card">
                <h3>Total Patients</h3>
                <div class="number" id="totalPatients">-</div>
            </div>
            <div class="card">
                <h3>Appointments</h3>
                <div class="number" id="totalAppointments">-</div>
            </div>
            <div class="card">
                <h3>Total Sales</h3>
                <div class="number" id="totalSales">-</div>
            </div>
        </div>

        <div style="text-align: center; margin-top: 30px;">
            <p><strong>Database:</strong> MySQL (opticpro)</p>
            <p><strong>Server:</strong> Node.js + Express</p>
            <p><strong>Status:</strong> Production Ready ✅</p>
        </div>
    </div>

    <script>
        // Load dashboard data
        fetch('/api/dashboard')
            .then(response => response.json())
            .then(data => {
                document.getElementById('status').textContent = data.message || 'Running';
                document.getElementById('totalPatients').textContent = data.totalPatients || 0;
                document.getElementById('totalAppointments').textContent = data.totalAppointments || 0;
                document.getElementById('totalSales').textContent = data.totalSales || 0;
            })
            .catch(error => {
                document.getElementById('status').textContent = 'API Connection Error';
                console.error('Error:', error);
            });
    </script>
</body>
</html>
HTMLEOF

echo "Frontend created! Your site should now load properly at opt.vivaindia.com"