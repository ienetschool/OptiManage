🚀 FINAL PRODUCTION SERVER COMMANDS
===================================

Copy and paste these commands in your SSH terminal:

ssh vivassh@5.181.218.15

# Stop all processes using port 8080
sudo fuser -k 8080/tcp
pkill -f 'tsx server/index.ts'
pkill -f 'node.*8080'

# Wait for processes to stop
sleep 5

# Navigate to project directory
cd /var/www/vhosts/vivaindia.com/opt.vivaindia.sql

# Start production server
NODE_ENV=production PORT=8080 DATABASE_URL='mysql://ledbpt_optie:g79h94LAP@5.181.218.15:3306/opticpro' tsx server/index.ts > production.log 2>&1 &

# Wait for server to start
sleep 10

# Check server status
ps aux | grep tsx | grep -v grep
netstat -tlnp | grep :8080

# Test API
curl -s http://localhost:8080/api/dashboard | head -c 100

# Check logs if needed
tail -10 production.log

✅ After running these commands:
- Production server will be accessible at opt.vivaindia.com
- All forms will work (patient registration, appointments, etc.)
- Both development and production use the same MySQL database
- No more 502 errors or form submission failures